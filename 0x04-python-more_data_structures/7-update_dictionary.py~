#!/usr/bin/python3

"""Function that prints a dictionary sorted by unordered keys"""


def print_sorted_dictionary(a_dictionary):

    sorted_keys = sorted(a_dictionary)
    for i in sorted_keys:
        print(f"{i}: {a_dictionary[i]}")

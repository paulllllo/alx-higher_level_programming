#!/usr/bin/python3

def safe_print_list(my_list=[], x=0):

    count = 0
    for i in range(0, x):
        try:
            print(f"{my_list[i]}")
            count++
        except:
            print("<x> is out of range of <my_list>")

    return count
